[project]
name = "qrcode-generator"
version = "0.1.0"
description = "Internal tool to build unique QR codes."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "pillow>=12.2.0",
    "qrcode[pil]>=8.2",
]
